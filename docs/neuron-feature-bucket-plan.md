# Moved

The neuron -> physiological feature-bucket plan now lives in the sibling repo, which owns
it outright:

**`../neuron-feature-mapping/docs/neuron-feature-bucket-plan.md`**
(GitHub: https://github.com/mendresvon/neuron-feature-mapping)

The plan itself was never published from this repository; it was drafted here and moved
before the first push. Only this pointer remains, deliberately. Two copies of a preregistered
design drift, and a preregistration that drifts is not one.

## This repo is frozen

`multimodal-causal-ablation` is read-only history as of 2026-08-19. It holds the upstream
thesis work, the scratched Table IX result, and 26 journal entries covering how the project
got here. Read it for that; do not edit it.

Everything the successor project needs has been copied there, including the MME2E source
and `base_model.pt`. Work happens in `neuron-feature-mapping`.
