# Lab Notebook — 2026-07-27

## 1. Objective & Hypothesis

My goal for this session was to set up the initial execution environment for Section VI-D of the paper and acquire the RML dataset. I needed to get a working Colab GPU runtime with PyTorch, download the raw data, and confirm that the model code could at least import without crashing.

## 2. Session & Runtime State

- **Hardware:** Google Colab, T4 GPU
- **PyTorch version:** Started with Colab's default (latest); downgraded to 2.2.2 during session
- **Working directory:** `/content/drive/MyDrive/` (early-stage, pre-restructuring)

## 3. Experimental Execution & Parameters

1. **Data acquisition:** Downloaded 3.2 GB of RML (Ryerson Audio-Visual Database) raw media files via Kaggle API. I used Colab secrets (`kaggle_api_token`) to authenticate without exposing credentials in the notebook. The dataset arrived as subject folders `s1` through `s8`, each containing `.avi` audio/video emotion recordings, under `data/Raw data/RML/`.

2. **GPU runtime configuration:** Confirmed CUDA availability on the Colab T4 instance.

3. **Initial model import attempt:** Tried to import the upstream model code from `src/`. This triggered the ABI crash described in Section 5.

## 4. Empirical Findings & Data Links

No quantitative results this session — the work was purely environmental setup. I confirmed the dataset directory layout matched expectations (8 subject folders with `.avi` files).

## 5. Roadblocks, Failure Modes & Debugging Log

- **PyTorch/torchaudio ABI crash:** On first attempt to import the model, I hit an `aoti_torch_abi_version` C++ runtime error. The root cause was a version mismatch between Colab's default PyTorch installation and `torchaudio`. I resolved it by pinning both `torch` and `torchaudio` to version `2.2.2`.

- **SparseConvNet C++ extension crash (unresolved):** After the PyTorch downgrade, `sparseconvnet` threw an `undefined symbol` ImportError. Its pre-compiled C++ shared libraries were built against a newer PyTorch ABI and couldn't link against 2.2.2. I decided to re-compile from source rather than upgrade PyTorch, since upgrading would break the legacy evaluation scripts in the codebase. This was left unresolved at session end.

## 6. Interpretability Retrospective

This was a setup day, so there's nothing to interpret scientifically. But the ABI debugging did surface an important constraint: the upstream codebase is tightly coupled to a specific PyTorch version, and any environment changes need to be tested carefully against the C++ extensions before running experiments.

## 7. Handoff Checklist for Next Runtime

- Re-build `sparseconvnet` from source inside the PyTorch 2.2.2 environment
- Confirm a clean forward pass through `main.py` with no import errors
- Begin locating pre-trained model checkpoints for base and fine-tuned models
