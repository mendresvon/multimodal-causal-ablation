# 2026-08-10 — Session 1

## 1. Objective & Hypothesis

The v3 notebook's data-integrity gate failed on Colab this morning with 97 problems across the 720 RML split IDs (missing `audio.wav` files, stray sync-conflict files like `audio (1).wav`, and frame counts that didn't match the sampled index). My working hypothesis, consistent with the warning already written into that assert message, was that this was a Google Drive stream-mount sync glitch on the Colab side rather than actual data loss — i.e. the underlying dataset was fine, but Drive's "stream files" mode hadn't finished materializing some files when the notebook read them. The goal of this session was to rule out corruption at the source and rebuild the Drive copy from a known-clean source so the next Colab run isn't reading from a folder with unknown history.

## 2. Session & Runtime State

- Hardware: local M2 MacBook Air (no GPU used this session; no Colab runtime was started)
- Git commit at session start: `d587172`
- Working directory: `/Users/breznev/Library/CloudStorage/GoogleDrive-mendresvon@gmail.com/My Drive/multimodal-causal-ablation`
- Data source for rebuild: `/Volumes/ADATA HD710 PRO/handover/data/Processed data/BIG_DATA_RAW_PROCESSED_FACE.tar.gz` (external HDD, not the flaky Drive mount)

## 3. Experimental Execution & Parameters

1. Deleted the existing (suspect) `BIG_DATA_RAW_PROCESSED_FACE` folder on Google Drive.
2. Built the combined list of all 720 RML split IDs (518 train + 58 valid + 144 test, deduplicated) and verified every one exists as a folder inside the external HDD's tar archive — single-pass `awk`-based top-level folder extraction, diffed against the ID list with `comm -23`, 0 missing.
3. Extracted exactly those 720 folders plus `meta_one_hot_label_six_categories.pkl` from the tar archive (selective extraction via `tar -xzf ... -T memberlist.txt`) into `/Users/breznev/Documents/Research Dataset/BIG_DATA_RAW_PROCESSED_FACE/`. Ran this as a background job since a full pass over the 42GB archive takes ~20+ minutes regardless of how many members are selected (gzip has no random access, so partial extraction still costs a full sequential decompress).
4. Verified the extraction: folder count 720/720 exact match against the ID list (`comm -23`/`comm -13` both empty), pkl present and non-empty (9.3MB), total size 1.2GB, spot-checked 3 folders for real per-utterance file contents (145–170 files each), no `(1)`-style duplicate filenames anywhere in the tree.
5. Uploaded the verified local copy to a new Google Drive folder via the web UI (not the desktop sync client, to avoid the stream-cache behavior implicated in the original failure).
6. Verified the Drive-side result directly via the Drive API: searched for the new `BIG_DATA_RAW_PROCESSED_FACE` folder, then listed all children by `parentId`. Got 737 rows back but only 721 unique file IDs — the extra 16 rows were the same object returned twice by the search API (pagination overlap on 16 items), not real duplicate folders. Confirmed by checking that the "duplicate" title/ID pairs shared identical IDs and `createdTime` values. Top-level count: 721 unique children (720 folders + 1 pkl), matching the local extraction exactly, no name collisions. Note: this only confirms the folder shells exist, not that their contents finished uploading (see below).
7. Initially misread the top-level count as proof the upload was finished. It was not — the Drive web-UI uploader creates folder objects before the files inside them finish uploading, so a correct top-level count doesn't mean the per-utterance files have landed. Caught this when I sampled two subfolders (`s3_f1eng_an2`, `s7_f1eng_sa5`) and found both completely empty. Re-checked those same two folders plus eight more sampled across different speaker/emotion prefixes about 15-40 minutes later: all ten now contain their expected `audio.wav`, `audio_16000.wav`, and `image_*.jpg` files, with creation timestamps clustered between 07:24 and 07:48 UTC today. This is consistent with the upload finishing in that window. Have not exhaustively checked all 720 folders — the 10-folder sample plus consistent recent timestamps is treated as strong evidence of completion, not certainty.

## 4. Empirical Findings & Data Links

No model was run this session — this was purely a data-provenance and integrity session. Key numbers:

- 720/720 RML split IDs present in the source archive (0 missing)
- 720/720 folders + 1 pkl extracted locally and verified (exact ID match, 1.2GB total)
- 721/721 unique top-level items confirmed on Google Drive (no duplicate folder objects, no stray `(1)`-suffixed files); 10/720 subfolders spot-checked for actual file contents, all 10 populated with timestamps in the 07:24-07:48 UTC window, treated as strong (not exhaustive) evidence the upload finished

## 5. Roadblocks, Failure Modes & Debugging Log

- Original Colab error: `AssertionError: HALT: 97 data-integrity problem(s) across the 720 RML split IDs`, with the first failures being missing `audio.wav` files, stray `audio (1).wav` / `audio_16000 (1).wav` files shifting frame counts, and sampled frames absent from disk. The `(1)`-suffix pattern is the classic Google Drive sync-conflict rename, which points at the Drive stream-mount rather than the dataset itself.
- A first attempt to sanity-check the tar extraction by pulling just 3 test files stalled for 5+ minutes before I realized gzip's lack of random access means a 3-file extraction costs the same full-archive scan as extracting all 720. Killed the test job and went straight to the real extraction instead of paying that cost twice.
- Mid-session I noticed the Google Drive desktop app (running in "stream files" mode) had started syncing images from the new folder while the web upload was still in progress, and paused it, concerned it might pull stale/cached copies into the fresh dataset. Checked directly via the Drive API rather than guessing: the new folder has exactly 721 unique children with no duplicates, so the upload itself is clean. Left the desktop app paused as a precaution until the next Colab run confirms the fix, since the original failure mode was specifically a stream-mount timing issue.

## 6. Interpretability Retrospective

Nothing about the model's representations to report this session — this was infrastructure work. The main takeaway is confirming that the original 97-problem failure was very likely environmental (Drive stream-sync timing) rather than a real gap in the RML data itself, since every one of the 720 IDs traced cleanly back to the source archive with correct file counts. This matters for the project's fidelity gate philosophy from ADR 0004: an assert that halts on suspicious data is only useful if I can distinguish "the data is actually broken" from "the mount hasn't caught up yet," and today's archive-level verification gives a clean local reference to compare against going forward.

## 7. Handoff Checklist for Next Runtime

- Resume from the v3 notebook's data-integrity gate cell (`data_integrity_problems(_all_ids)` / the assert immediately after it) and re-run against the new `BIG_DATA_RAW_PROCESSED_FACE` Drive folder.
- No cached activations or checkpoints to reload — this session didn't touch the model.
- Open item: keep the Google Drive desktop sync client paused until the integrity gate passes cleanly on Colab, then resume it. If the gate still fails after this rebuild, the Drive-side data is now confirmed clean, so the next suspect is the Colab mount/runtime itself, not the dataset.
