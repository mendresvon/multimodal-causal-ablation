# 2026-07-30 (Session 2)

### 1. Objective & Hypothesis

I opened a fresh AI agent session today intending to start Phase A, and the agent immediately went off the rails — it understood the experiment phases correctly but started creating standalone Python files (`src/phase_a.py`) instead of writing code in the notebook. The agent had no awareness of my infrastructure decisions. My hypothesis was that a project-scoped rules file would solve this by giving every new session the full operational context on startup.

### 2. Session & Runtime State

- **Hardware:** Local M2 MacBook Air (no GPU work)
- **Git commit at session start:** `178ee0a`
- **Working directory:** `multimodal-causal-ablation/`

### 3. Experimental Execution & Parameters

No experiment execution. I ran a grilling session to design and implement an AI agent instruction file (`.agents/AGENTS.md`) that prevents future sessions from going rogue. Key decisions resolved:

1. **Placement:** `.agents/AGENTS.md` — the standard location that AGY IDE automatically loads for every new session. Already covered by the existing `.gitignore` rule for `.agents/`.

2. **Primary execution surface rule:** The notebook is the main file. Code defaults to notebook cells. Standalone files are permitted only when genuinely warranted (shared utilities), not as the default.

3. **7-section structure:** Project Identity, Execution Surface, Notebook Convention, Phase Roadmap (live status table), Key Files Reference, Infrastructure Rules, Tone & Style.

4. **Live phase roadmap:** A status table in `AGENTS.md` that gets updated during session teardown alongside the journal entry. The agent reads this first to know what phase is active, then reads the latest journal handoff checklist for exact next steps.

Also restored `src/utils.py` (deterministic seed utility) which was deleted by the rogue agent session.

### 4. Empirical Findings & Data Links

No empirical results. Outputs are structural:
- `.agents/AGENTS.md` — project-scoped AI agent rules (gitignored)
- `src/utils.py` — restored deterministic seed utility

### 5. Roadblocks, Failure Modes & Debugging Log

- **Rogue agent behavior:** A fresh AI agent session created `src/phase_a.py` instead of writing Phase A code in the notebook. Root cause: the agent had no project-scoped context file telling it where to write code or what infrastructure conventions existed. It defaulted to standard software engineering habits (one file per module) instead of the Colab notebook workflow.
- **`src/utils.py` deleted:** The rogue session also deleted the deterministic seed utility, likely while restructuring the `src/` directory for its standalone file approach.

### 6. Interpretability Retrospective

The rogue agent incident is actually a useful lesson about context and assumptions. The agent "knew" the experiment (it read the handoff summary and ADR 0001) but didn't know the *operational constraints* — that this is a Colab notebook project, not a standard Python package. It's the same kind of error a new collaborator would make if you handed them the paper but not the lab manual. The `AGENTS.md` file is essentially a lab manual for AI collaborators.

### 7. Handoff Checklist for Next Runtime

- Connect `multimodal-causal-ablation.ipynb` to Google Colab GPU runtime
- Phase A: Load `base_shap.pkl` and `finetuned_shap.pkl`, compute aggregated SHAP attribution per modality, and formally verify Audio as the Dominant Modality
- If confirmed, proceed to Phase B: register forward hooks on `model.a_transformer`, extract activation tensors, fit L1-logistic regression probes, check mean AUC against 0.65 threshold
