# 2026-07-30 (Session 1)

### 1. Objective & Hypothesis

I needed experiment infrastructure in place before touching any Phase A–E code. The Colab ephemeral runtime problem, the lack of version control, the absence of a journaling standard, and the multi-week timeline all pointed to the same conclusion: if I started running experiments without a backbone, I would lose work, forget decisions, and produce results I couldn't reproduce. Tonight's goal was to build that backbone from scratch.

### 2. Session & Runtime State

- **Hardware:** Local M2 MacBook Air (no GPU work, purely organizational)
- **PyTorch version:** N/A (no model execution this session)
- **Git commit at session start:** No repository existed
- **Git commit at session end:** `178ee0a` (2 commits on `main`)
- **Working directory:** `~/Library/CloudStorage/GoogleDrive-<account>/My Drive/multimodal-causal-ablation/`

### 3. Experimental Execution & Parameters

No experiments were executed. This entire session was dedicated to infrastructure decisions and implementation. I ran a structured grilling session that resolved 9 decisions across 4 branches, then implemented all of them:

1. **Git repository initialization** with a strict `.gitignore` policy separating heavy binaries (~3.5 GB of datasets and model weights) from trackable code, documentation, and lightweight results.

2. **Provenance manifests** (`checkpoints/README.md`, `data/README.md`) with SHA-256 checksums computed against the actual checkpoint files:
   - `base_model.pt`: `565bc220...`
   - `finetuned_model.pt`: `a4c1707f...`
   - `base_shap.pkl`: `60fbd3c7...`
   - `finetuned_shap.pkl`: `4211cf2e...`

3. **Scientific journaling system** with a codified 7-part schema (Objective, Runtime State, Execution, Findings, Roadblocks, Retrospective, Handoff), enforced through an internal `journals/GUIDELINES.md` that is deliberately excluded from the public GitHub repo.

4. Artifact storage architecture: `results/` for Git-tracked CSV/JSON metrics, `figures/` for publication plots, `checkpoints/activations/` for ephemeral cached tensors on Google Drive.

5. **Deterministic seed utility** (`src/utils.py`) pinning all randomness sources (`torch`, `numpy`, `random`, `cudnn`) to `seed=0`.

6. **Pinned dependencies** (`requirements.txt`) locked to PyTorch 2.2.2 for C++ ABI compatibility.

7. Domain model update: Reframed all `_Avoid_` directives in `CONTEXT.md` to `_Distinguish from_` for scholarly disambiguation.

8. **Rewrote both existing journal entries** (2026-07-27, 2026-07-29) to conform to the new 7-part schema. Critically, I flagged the Dominant Modality conclusion from the July 29 entry as preliminary: the loss curve inspection was done before any protocol existed and needs formal re-verification via aggregated SHAP attribution in Phase A.

9. **ADR 0002** documenting all infrastructure decisions with rationale.

10. Internal file audit: Identified and `.gitignore`'d files that would look unprofessional on a public GitHub repo (AI agent config, unpublished paper draft, advisor communications, internal writing guidelines).

11. **GitHub repository created** as `multimodal-causal-ablation` with description: *"Neuron-level causal validation using linear probes and activation ablation for multimodal emotion recognition."* Renamed the notebook and all internal path references to match.

### 4. Empirical Findings & Data Links

No empirical results this session. All outputs are structural:
- Root `README.md`: grad-school-application-ready project overview
- `docs/adr/0002-infrastructure-and-journaling-protocol.md`: infrastructure decision record
- `src/utils.py`: deterministic seed utility
- `requirements.txt`: pinned dependency manifest

### 5. Roadblocks, Failure Modes & Debugging Log

- **`.gitignore` negation rule for `data/README.md`:** The initial `data/` exclusion pattern caught `data/README.md` as well. Fixed by switching to `data/*` with a `!data/README.md` negation rule and verifying with `git add -n`.
- **Upstream training artifacts almost tracked:** `Model/Dig-Data_Model-Main/savings/` and `tensorboard/` directories (upstream training logs) were not in the initial `.gitignore`. Caught during the verification pass and added exclusion rules.

### 6. Interpretability Retrospective

No neural network interpretability work was done tonight, but the infrastructure choices themselves reflect interpretability thinking. The 7-part journal schema forces me to separate observation (Section 4) from interpretation (Section 6), the same distinction that separates correlation from causation in the experiment itself. The Dominant Modality flag is a good example: the loss curve numbers are observations; the claim that "Audio is dominant" is an interpretation that requires the SHAP attribution protocol from ADR 0001 to properly test.

### 7. Handoff Checklist for Next Runtime

- Connect `multimodal-causal-ablation.ipynb` to Google Colab GPU runtime
- Phase A: Load `base_shap.pkl` and `finetuned_shap.pkl`, compute aggregated SHAP attribution per modality, and formally verify Audio as the Dominant Modality
- If confirmed, proceed to Phase B: register forward hooks on `model.a_transformer`, extract activation tensors, fit L1-logistic regression probes, check mean AUC against 0.65 threshold
