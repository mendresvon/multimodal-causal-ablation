# 2026-08-04 (Session 3)

### 1. Objective & Hypothesis
I aimed to run the full-dataset feature extraction (`SHAP_feature()`) for both the base and fine-tuned models to resolve the P1 fix (Phase A). This is required to evaluate downstream causal ablations on the full 723-sample dataset rather than just the 144 test samples.

### 2. Session & Runtime State
- **Hardware:** Google Colab GPU (T4/V100)
- **PyTorch version:** 2.2.2
- **Working directory path:** `/content/drive/MyDrive/multimodal-causal-ablation`

### 3. Experimental Execution & Parameters
- Loaded the full RML dataset (train+val+test = 723 samples).
- Rewrote the feature extraction script in cell 3.5.1 to implement incremental batch saving (checkpointing). Instead of holding all representations in Colab RAM until the end, it now securely saves `batch_features_{idx}.npy` and `batch_labels_{idx}.npy` directly to Google Drive after every iteration.
- Initiated the forward pass on the Colab GPU.

### 4. Empirical Findings & Data Links
The batch-saving logic worked flawlessly. The model successfully extracted and securely cached **73 out of 90 batches** (over 80% of the dataset) for the base model before execution was halted. 
- Cached chunks are located at: `checkpoints/activations/tmp_base_features/`

### 5. Roadblocks, Failure Modes & Debugging Log
The forward pass for the full dataset takes ~10-30 minutes per model. I ran out of Colab compute quota and the session was abruptly disconnected. However, because I implemented incremental checkpointing in cell 3.5.1, the crash did not result in data loss. 73 batches were permanently saved and the script is designed to automatically resume from batch 73 upon the next execution.

### 6. Handoff Checklist for Next Runtime
- [x] Implement intermediate batch saving (checkpointing) in cell 3.5.1 so data is not lost on disconnect.
- [ ] Resume cell 3.5.1 with a fresh Colab GPU quota to finish the remaining 17 batches and compile `base_full_1152.npy`.
- [ ] Run the extraction for the fine-tuned model (`ft_full_1152.npy`).
- [ ] Proceed to Phase B once activations are successfully cached.
