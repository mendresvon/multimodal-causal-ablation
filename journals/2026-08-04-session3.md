# 2026-08-04 (Part 3)

### 1. Objective & Hypothesis
I aimed to run the full-dataset feature extraction (`SHAP_feature()`) for both the base and fine-tuned models to resolve the P1 fix (Phase A). This is required to evaluate downstream causal ablations on the full 723-sample dataset rather than just the 144 test samples.

### 2. Session & Runtime State
- **Hardware:** Google Colab GPU (T4/V100)
- **PyTorch version:** 2.2.2
- **Working directory path:** `/content/drive/MyDrive/multimodal-causal-ablation`

### 3. Experimental Execution & Parameters
- Loaded the full RML dataset (train+val+test = 723 samples).
- Wrote code to extract `base_full_1152.npy` and `ft_full_1152.npy` for the base and fine-tuned models respectively.
- Initiated the forward pass on the Colab GPU.

### 4. Empirical Findings & Data Links
No findings. The forward pass was interrupted and terminated early due to Colab usage constraints before saving any data to the drive.

### 5. Roadblocks, Failure Modes & Debugging Log
The forward pass for the full dataset takes ~10-30 minutes per model. Due to Colab usage limits running low, the session was aborted. The current notebook script stores all features in memory and saves them only at the very end of the extraction process. Consequently, all progress is lost upon disconnection.

### 6. Handoff Checklist for Next Runtime
- [ ] Implement intermediate batch saving (checkpointing) in cell 3.5.1 so data is not lost on disconnect.
- [ ] Re-run the full-dataset feature extraction (cell 3.5.1 and 3.5.2) with a fresh Colab GPU quota.
- [ ] Proceed to Phase B once activations are successfully cached.
