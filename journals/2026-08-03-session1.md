# Lab Notebook — 2026-08-03

> **⚠️ ERRATUM:** The quantitative findings and conclusions in this entry were invalidated by a subsequent methodology audit which discovered dataset truncation and dimensionality mismatches. See the master audit report and `2026-08-04-session2.md` for the correction.

## 1. Objective & Hypothesis
My goal today was to prepare for Phase C (Causal Ablation) and to fix a recurring problem where the AI agent failed to follow the notebook structure convention (every code cell must be preceded by a markdown cell). Due to time constraints, no Colab GPU execution was performed.

## 2. Session & Runtime State
- **Hardware:** Local M2 MacBook Air (no Colab GPU session today)
- **Working directory:** `/Users/breznev/Library/CloudStorage/GoogleDrive-mendresvon@gmail.com/My Drive/multimodal-causal-ablation/`

## 3. Experimental Execution & Parameters
No experiment code was executed. The session focused on two preparatory tasks:
1. Drafting the Phase C notebook cells for class-selectivity ratio computation and mean-ablation hooks.
2. Strengthening the agent rules in `AGENTS.md` to prevent future notebook structure violations.

## 4. Empirical Findings & Data Links
No empirical data was generated today as the Colab GPU runtime was not invoked.

## 5. Roadblocks, Failure Modes & Debugging Log
- **Recurring Agent Rule Violation:** Across multiple sessions, the AI agent consistently delivered bare Python code blocks without the mandatory preceding markdown cell. Root cause: the "Notebook Editing" subsection in `AGENTS.md` told agents to provide "clear instructions" but never defined the delivery format. I added an explicit delivery format rule with correct/wrong examples to `AGENTS.md` Section 7 ("Notebook Editing") to close this gap.
- **Model Instantiation Requirement:** Unlike Phases A and B (which relied solely on cached SHAP representations), Phase C will require instantiating the actual PyTorch `MME2E` models to measure the causal accuracy drop after ablation. The selectivity ratio computation itself only needs numpy on the cached 1024-d Tier 1 activations.

## 6. Interpretability Retrospective
By preparing the Phase C logic offline, I can separate the conceptual design of the ablation sweep from the expensive GPU runtime. The selectivity ratio computation (mean(n|c) / mean(n|~c)) is pure numpy and does not need a GPU. The model-dependent part (measuring accuracy drops after neuron clamping) will require a Colab session with the full MME2E forward pass.

## 7. Handoff Checklist for Next Runtime
- **Resume from:** Phase C, Step 1 — compute class-selectivity ratios for the 1024-d Text Tier 1 representation.
- **Cached artifacts to load:** `checkpoints/activations/base_acts_tier1.npy`, `checkpoints/activations/ft_acts_tier1.npy`, and labels derived from the RML test split metadata (as established in Phase B, notebook Section 4).
- **What to do:** Create the Phase C notebook cells following ADR 0001 Section 3 (Causal Ablation & Selectivity Threshold). For each of the 6 emotion classes, compute the selectivity ratio mean(n|c) / mean(n|~c) across all 1024 neurons, rank them, and identify the top k={1,3,5,10} most selective neurons. Then perform mean-ablation sweeps: clamp those top-k neurons to their dataset-mean values during inference and measure the per-class accuracy drop. A feature set is causally class-selective if the target class accuracy drop is >= 2.5x the mean absolute non-target class drop.
- **Unresolved blocker:** Need to determine the correct PyTorch hook registration point inside `MME2E` for clamping the 1024-d text CLS neurons during inference. Inspect the model architecture at the start of the Colab session.
