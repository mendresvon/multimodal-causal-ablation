# Lab Notebook: 2026-08-04

> **⚠️ ERRATUM:** The quantitative findings and conclusions in this entry were invalidated by a subsequent methodology audit which discovered dataset truncation and dimensionality mismatches. See the master audit report and `2026-08-04-session2.md` for the correction.

## 1. Objective & Hypothesis
The objective today was to execute Phase C (Causal Ablation) to empirically test whether the 1024-d Text Tier 1 representation developed causally class-selective neurons for the 6 emotion classes. My hypothesis was that a subset of highly selective neurons would degrade classification accuracy when mean-ablated by enough to clear the 2.5x threshold.

## 2. Session & Runtime State
- **Hardware:** Google Colab GPU
- **PyTorch version:** 2.2.2 (pinned via `requirements.txt`)
- **Git commit at session start:** `21e1541e82286e2bf0ed5079f8ed9c07632fd7e9`
- **Working directory:** `/content/drive/MyDrive/multimodal-causal-ablation/`
- **Dependencies:** Required manual installation of `facenet-pytorch` and `transformers` in the ephemeral runtime.

## 3. Experimental Execution & Parameters
To execute the ablation sweep efficiently without reloading the raw dataset and massive modality encoders (ALBERT, MTCNN, etc.), I designed a "proxy inference" loop. I loaded the 1152-d `test_feature` tensors directly from the cached SHAP data, split out the 1024-d Text CLS token, injected dataset-mean clamp values for the top-k neurons, and fed the modified vectors straight into `MME2E`'s pre-trained classification heads (`t_out`, `v_out`, `a_out`, `weighted_fusion`). I swept $k \in \{1, 3, 5, 10\}$.

## 4. Empirical Findings & Data Links
The results at $k=5$ (primary target for Tables VII/VIII) showed almost zero accuracy drop across the board.
- **Base Model:** Only `surprise` proved causally selective (4.17% target drop, 0.0% non-target drop). All other classes exhibited 0.00% target accuracy drop.
- **Fine-tuned Model:** 0.00% target accuracy drop across all 6 emotion classes. The model completely failed the causal selectivity threshold.

Artifacts:
- `results/phase_c_k5_ablation_results.csv`
- `results/phase_c_base_ablation_sweep.json`
- `results/phase_c_finetuned_ablation_sweep.json`

## 5. Roadblocks, Failure Modes & Debugging Log
- **Missing Dependencies:** The Colab runtime threw a `ModuleNotFoundError` for `facenet_pytorch`. I resolved this by adding a `!pip install` cell at the top of the notebook referencing the pinned `requirements.txt` and `transformers`.
- **Logic Bug in Selectivity Threshold:** My initial Python logic for the ADR 0001 threshold was `target_drop >= 2.5 * mean_nt_drop`. Because both values were exactly 0.0 for many classes, Python evaluated `0.0 >= 0.0` as `True`, generating false positives. I corrected the inequality to explicitly require `target_drop > 0`.

## 6. Interpretability Retrospective
Clamping the 5 most class-selective neurons, ranked by activation ratio, out of the 1024-d representation produced effectively no change in model behaviour. Read at face value that points to distributed, entangled representations rather than sparse "grandmother neurons", and fine-tuning appears to push the dispersion further still, removing the one causally selective set (`surprise`) present in the base model. I am not confident in that reading yet. A null this uniform is also what a mistargeted hook produces, and I have no positive control in the pipeline that would tell the two apart.

## 7. Handoff Checklist for Next Runtime
- **Resume from:** Phase D, Transfer Retention Analysis. Create new notebook cells starting at **Section 9** after the current Section 8 in `multimodal-causal-ablation.ipynb`. Add a top-level `# Phase D, Transfer Retention Analysis` heading cell first.
- **Cached artifacts to load:** `results/phase_c_base_ablation_sweep.json`, `results/phase_c_finetuned_ablation_sweep.json`, and the in-memory variables `base_top_neurons`, `ft_top_neurons`, `base_acts_tier1`, `ft_acts_tier1`, `labels_tier1` (all populated by running Sections 6-8).
- **Unresolved blockers:** None. However, the following remediation items should be addressed before Phase D execution:
  1. Phase B has no saved CSV artifact in `results/`: add `df.to_csv()` export to the Phase B notebook cell.
  2. Phase C notebook code saves JSON sweeps but not the CSV summary: add `df.to_csv()` for `phase_c_k5_ablation_results.csv`.
  3. The `ratio` column displays `4166666.67` for `base/surprise` due to epsilon division when `mean_nt_drop == 0`: cap or format as `inf`.
  4. Two orphaned code cells in the notebook (Cell 2: Drive mounting, Cell 10: verdict computation) lack preceding markdown cells.
