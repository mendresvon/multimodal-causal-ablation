# Lab Notebook — 2026-08-02

## 1. Objective & Hypothesis
My goal for this session was to execute Phase B (Probe Signal Validation) targeting the dominant Text modality established in Phase A. I planned to fit L1-regularized logistic regression probes on the Text branch's FFN activations (Tier 1) to verify class-discriminative signal retention (Mean AUC >= 0.65).

## 2. Session & Runtime State
- **Hardware:** Local M2 MacBook Air & Google Colab GPU
- **PyTorch version:** PyTorch 2.2.2 (via `requirements.txt`)
- **Git commit at session start:** `a58988d` (followed by an intermediate commit `87c53fe` fixing ADR 0001)
- **Working directory:** `/Users/breznev/Library/CloudStorage/GoogleDrive-mendresvon@gmail.com/My Drive/multimodal-causal-ablation/`

## 3. Experimental Execution & Parameters
1. **Resume Gate:** Confirmed the presence of precomputed `base_acts.npy`, `ft_acts.npy`, and `base_labels.npy` in `checkpoints/activations/`.
2. **In-Sample Probe (Failed attempt):** Ran a naive L1-logistic regression on the loaded activations without cross-validation, resulting in artifactual 1.000 AUCs across all classes due to `p > n` overfitting (512 dimensions vs 288 samples).
3. **Out-of-Fold Probe:** Updated the Colab notebook to use 5-fold `StratifiedKFold` and `cross_val_predict` to obtain honest out-of-sample probability estimates.
4. **ADR Update:** Modified `docs/adr/0001-causal-validation-methodology.md` to correctly reflect a 2-tier layer fallback hierarchy (FFN output -> Encoder CLS) after identifying the model uses late-fusion.

## 4. Empirical Findings & Data Links
The out-of-fold validation on the cached 512-d activations yielded near-random performance:

**Base Model Probe:**
- Mean AUC: 0.4787
- Classes < 0.55 AUC: 6
- Verdict: [WARNING] Signal Validation FAILED

**Fine-tuned Model Probe:**
- Mean AUC: 0.4889
- Classes < 0.55 AUC: 5
- Verdict: [WARNING] Signal Validation FAILED

Both models failed the Phase B sanity check under the current cached activations, triggering a formal layer fallback condition.

## 5. Roadblocks, Failure Modes & Debugging Log
- **Architectural Error (Late Fusion):** An urgent report clarified that MME2E takes a weighted sum of the 6-class classification scores. There is no intermediate "post-fusion representation" vector. I corrected `ADR 0001` to remove Tier 2 (Post-fusion) and fall back directly to the Encoder CLS layer.
- **Overfitting Trap:** Initially fitting the L1 probe on the entire test set generated 1.000 AUCs. I diagnosed this as the curse of dimensionality and corrected it using cross-validation.
- **Anomalous Activation Shape:** The `base_acts.npy` and `ft_acts.npy` loaded with shape `(288, 512)`. However, the 'large' ALBERT Text configuration specifies a 1024-d output (`text_cls_dim=1024`). The `512` dimension matches the intermediate flattening steps of the Audio/Video branches (`v_flatten`/`a_flatten`).

## 6. Interpretability Retrospective
The complete lack of signal (Mean AUC ~ 0.48) in the currently cached activations is almost certainly an artifact of the anomalous `(288, 512)` tensor shape. If these activations were accidentally extracted from the `v_flatten` or `a_flatten` (visual/acoustic) branches instead of the `T` (text) branch, the probes predicting text-dominant emotion cues would logically fail. Before formally falling back to the Tier 2 (Encoder CLS) text representations, I suspect we must regenerate the correct 1024-d Tier 1 FFN outputs directly from `model.t_out` input to see if a valid text signal exists.

## 7. Handoff Checklist for Next Runtime
- Investigate how `base_acts.npy` (512-d) was generated. We need to write PyTorch forward hooks to cleanly extract the 1024-d input to `model.t_out`.
- Update the Colab notebook to generate and cache the correct 1024-d activations in `checkpoints/activations/`.
- Re-run the Phase B `cross_val_predict` validation cell on the new 1024-d activations to determine if Tier 1 truly fails or if the 512-d cache was just a bug.
